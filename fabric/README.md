# Fabric item definitions

Place source-controlled Fabric item definitions in this directory when using the pipeline templates in this repository. Keep this starter repository free of tenant-specific artifacts, data, credentials, and workspace IDs.

The validation script can require deployable item files when it runs in a real solution repository.
